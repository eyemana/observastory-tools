{
  "name": "observastory-tools",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/verify.mjs",
    "verify": "npm run check && npm test"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "openai": "^6.45.0"
  }
}
